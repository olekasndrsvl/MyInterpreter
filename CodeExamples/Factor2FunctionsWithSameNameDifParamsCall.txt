def f(integer : a) : integer
  {
  return 12.0;
    };
  
def f(bool : x) : double
  {
return 12.5;
    };
def factor(x)
  {
    if(x>10) then
      return 1;
    return factor(x+1)*2;
    }
{
  var x = f(1);
  print(x);
  print(f(x<2));
  print(factor(2));
  }